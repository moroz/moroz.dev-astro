<div class="landing">
  <section class="hero">
    <h2 class="title">
      Building the future.<br />
      One line of code at a time.
    </h2>
    <p class="subtitle">
      My name is Karol Moroz.<br />I am a software developer based in Poznań,
      Poland.
    </p>
  </section>

  <section class="links">
    <a href="https://github.com/moroz" target="_blank" nofollow class="github"
      >Follow me on Github</a
    >
    <a href="/blog" class="projects">Read my blog</a>
  </section>
  <img src="/km.webp" class="km" />
</div>

<style lang="scss">
  @import "@styles/_variables.sass";

  .landing {
    position: relative;
    height: calc(100vh - #{$header-height});
    padding-top: 1.5rem;
    overflow: hidden;
    background-image: linear-gradient(
        rgba(255, 255, 255, 0.3),
        rgba(255, 255, 255, 0.3)
      ),
      url(/bg.jpg);

    background-position: center;

    @include desktop {
      padding-top: 3rem;
    }

    @include dark {
      background-image: url(/bg-dark.jpg);
    }
    background-size: auto, cover, 20px;
  }

  .hero {
    margin-top: 0;
  }

  .hero,
  .links {
    text-align: center;
  }

  .title {
    @include fluid-type(rem-calc(768), rem-calc(1280), 1.5rem, 3rem);
    line-height: 1.125;
    margin-bottom: 0;

    @include lt-tablet {
      font-size: 1.5rem;
    }
  }

  .subtitle {
    margin: 0.75em 0 1.5rem;
    font-weight: 500;
    color: var(--emph-text);
    font-size: 0.9em;
  }

  a.github {
    background: var(--body-bg);
    color: var(--body-fg);
  }

  a.projects {
    color: white;
    background: var(--primary);
  }

  .links {
    font-weight: 600;
    font-size: 0.9em;

    display: flex;
    justify-content: center;
    gap: 1em;

    a {
      display: inline-flex;
      align-items: center;
      height: 2.5rem;
      padding: 0 1em;
      border: 1px solid var(--border);
      box-shadow: 0 5px 25px transparentize($primary, 0.5);

      @include desktop {
        height: 2.75rem;
      }
    }
  }

  .km {
    height: 70vh;
    position: absolute;
    bottom: 0;
    right: 3rem;

    @media screen and (max-width: #{rem-calc(1300px)}) {
      height: 60vh;
    }

    @media screen and (max-width: #{rem-calc(450px)}) {
      height: 50vh;
    }

    filter: saturate(120%) drop-shadow(0 0 20px transparentize($primary, 0.5));
  }
</style>
