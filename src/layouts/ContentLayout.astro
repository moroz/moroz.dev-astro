---
export interface Props {
  title: string;
  class?: string;
}

import BaseLayout from "./Layout.astro";
const { title, class: className } = Astro.props;
---

<BaseLayout title={title}>
  <main class:list={["container", className]}>
    <h1>{title}</h1>
    <slot />
  </main>
</BaseLayout>
